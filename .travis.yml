language: python
python:
  - "2.7"

before_script:
  - cp conf.travis.py conf.py
  - psql -U postgres schema.sql

# command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script: nosetests
